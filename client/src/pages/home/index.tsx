import React from "react";
import TodoList from "../_components/TodoList";

const Home: React.FC = () => {
  return (
    <div className="app">
      <TodoList />
    </div>
  );
};

export default Home;
